import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF0061FF);
  static const Color primaryTextColor = Color(0xFFFFFFFF);
  static const Color textOnboardingPage = Color(0xFF0061FF);
  // Colors Name
  static const Color textSesionClose = Colors.red;
  static const Color primaryCircleUser = Colors.grey;
  static const Color textProfileUserPage = Colors.black;

  // Colors RGB
  // static const Color textProfileUserPage = Color.fromARGB(255, 0, 0, 0);
  // Azul con 4% de opacidad
  static const Color accentColor = Color(0xFF0061FF);
}
